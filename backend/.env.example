DB_HOST=localhost
DB_USER=root
DB_PASS=enter-your-db-password
DB_NAME=omw_db
DB_PORT=3306

BASE_URL1=/api
JWT_SECRET=67f6d211fde663aee6f8091e7abf7330
JWT_EXPIRATION=5h

# AWS S3 configuration for presigned URL uploads and private document access
AWS_REGION=ap-south-1
# Primary bucket for provider documents (used when generating URLs)
S3_BUCKET=your-s3-bucket-name
# (Optional) legacy variable supported by code if you want a specific bucket var
S3_BUCKET_PROVIDER_DOCS=
# Credentials for your S3Uploader IAM user (do not commit real values)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key